# This file is a template, and might need editing before it works on your project.
# Lifted from: https://about.gitlab.com/2016/03/10/setting-up-gitlab-ci-for-ios-projects/
# This file assumes an own GitLab CI runner, setup on a macOS system.

stages:
  - build

build_project:
  stage: build
  script:
    - xcodebuild clean -workspace Payculator.xcworkspace -scheme Payculator | xcpretty
    - xcodebuild test -workspace Payculator.xcworkspace -scheme Payculator -destination "platform=iOS Simulator,name=iPhone XS Max,OS=12.0" | xcpretty -s
  